@using System.Web.Hosting
@{
    var themePath = WebPagesHtmlHelpers.MapPath("Layouts");
    
    if (HostingEnvironment.VirtualPathProvider.DirectoryExists(themePath))
    {
        Themes.Initialize(themePath, "Default");
    }
}